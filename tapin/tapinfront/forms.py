#from tapinfront.models import *
from django import forms
import uuid
'''
class LoginForm(forms.ModelForm):
    pin = forms.CharField(widget=forms.PasswordInput, max_length=6)   
    uid = forms.UUIDField()
    hostname = forms.CharField(max_length=30)
    session_token = forms.CharField(max_length=100)
    widgets = {
        'pin': forms.PasswordInput()
    }
    class Meta:
        model = User
'''
