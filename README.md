# TapIn

## Client Installation (`tapd`):
- Install `swig` with your package manager of choice.
- Create a virtual environment with `python3 -m virtualenv --python=/usr/bin/python3 venv`.
- Activate the virtual environment with `source venv/bin/activate`.
- Install the Python dependencies with `pip install -r requirements.txt`.
